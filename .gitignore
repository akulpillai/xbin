# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environments
.env
.venv
venv/
ENV/
env.bak/
venv.bak/

# Project Specific Data
uploads/*
!uploads/.gitkeep
signatures/*
!signatures/.gitkeep

# Docker
.dockerignore

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Redis
dump.rdb
appendonly.aof

# Test artifacts
tests/.orchestrator.log
e2e_*.log
orchestrator_*.log

# Repo-local scratch on the big disk (build staging, orchestrator temp/log)
.xbin_scratch/

# Server-side reference-binary library (large ELFs stay local)
references/*
!references/.gitkeep

# Local test firmware (pysyndy sample); keep out of the public fork
/sample.axf

# Persistent worker analysis caches (ghidriff diff / fid ghidra proj / bind_se sigdb)
cache/
